{
  "name": "landing-cv-vue",
  "version": "0.1.0",
  "homepage": "https://vadym4che.github.io/cv/",
  "private": true,
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "autodeploy": "npm run build && git checkout gh-pages && rm -rf * && mv dist/* . && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin gh-pages && git checkout main",
    "gh-pages-deploy": "node scripts/gh-pages-deploy.js",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "execa": "^8.0.1",
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@babel/core": "^7.12.16",
    "@babel/eslint-parser": "^7.12.16",
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-eslint": "~5.0.0",
    "@vue/cli-service": "~5.0.0",
    "eslint": "^7.32.0",
    "eslint-plugin-vue": "^8.0.3",
    "gh-pages": "^4.0.0"
  }
}
